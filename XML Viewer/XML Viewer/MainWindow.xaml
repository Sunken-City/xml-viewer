<Window x:Class="XML_Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="XML Viewer" Height="350" Width="525" Name="XML_Viewer" WindowStyle="ThreeDBorderWindow" Loaded="XML_Viewer_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Vertical">
            <Border BorderBrush="Blue" BorderThickness="2">
                <StackPanel Orientation="Horizontal">
                    <TextBlock>The menu goes here...</TextBlock>
                </StackPanel>
            </Border>
            <TreeView  HorizontalAlignment="Left" Margin="0,31,0,0" Name="treeView1" VerticalAlignment="Top">

            </TreeView>
            <Ellipse Height="25" HorizontalAlignment="Left" Name="ellipse1" Stroke="Black" VerticalAlignment="Top" Width="25" Margin="8,3,0,0" MouseLeftButtonDown="ellipse1_MouseLeftButtonDown" MouseLeftButtonUp="ellipse1_MouseLeftButtonUp" />
        </StackPanel>
    </Grid>
</Window>
